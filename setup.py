from setuptools import setup

setup(
    name='aws-ami-region-mapper',    # This is the name of your PyPI-package.
    version='0.1',                   # Update the version number for new releases
    scripts=['aws_ami_region_mapper']           # The name of your scipt, and also the command you'll be using for calling it
)
